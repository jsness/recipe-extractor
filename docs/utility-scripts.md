# Utility Scripts

| Script | Description |
|---|---|
| `.\scripts\dev.ps1` | Start Postgres, Go server, and Vite dev server in one command |
| `.\scripts\clear-db.ps1` | Wipe all data from local Postgres (prompts for confirmation) |
